# SW30 Labs brand assets

Canonical copies of the SW30 Labs monogram and Aineko mark. Matching files also live under `docs/assets/` for GitHub Pages.

| File | Use |
| --- | --- |
| `sw30-logo-mark.png` | Monogram / icon (org avatar, favicon-style mark, compact headers) |
| `sw30-logo-lockup.png` | Full lockup for profile README and wide headers |
| `aineko.svg` | Aineko companion mark (stroke SVG, `#c44928`) |

Prefer linking Pages/docs from `docs/assets/`. Prefer raw GitHub URLs from `docs/assets/` in Markdown that must render on github.com.
